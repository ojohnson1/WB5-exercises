"use strict"


let house ={

    type:'Detached',
    build:"brick",
    year:2019,
    price:250000,
    driveway:true,
}

for ( build in house){

    console.log(`This ${x} is made of ${x[i].build} it was made in the year ${x[i].year}  `)
}